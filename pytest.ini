[pytest]
testpaths = tests

# Integration tests auto-skip when INTEGRATION_BASE_URL is not set.
# To run them explicitly:
#   INTEGRATION_BASE_URL=https://sh4r3d.com INTEGRATION_TOKEN=<tok> pytest tests/integration/ -v
#
# To run only unit tests (exclude integration):
#   pytest tests/ --ignore=tests/integration
